#include <stdlib.h>
#include <time.h>
int main (void)
{
	int n;

	srand(time(0);
	n = rand() - RAND_MAX / 2;
	if (0 > n) {
	printf("0 is positive\n");
	} 

	if (0 < n){
	printf("is negative")
        }
	else printf(is zero);{}	
